import { CollectionConfig } from 'payload/types'

const Test: CollectionConfig = {
    slug: 'test',
    fields: [
        {
            name: 'name',
            type: 'text'
        }

    ],
}

export default Test
